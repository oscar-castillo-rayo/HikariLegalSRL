@{
    ViewData["Title"] = "Prospectos";
}

<h1>Prospectos</h1>

@if (TempData["Mensaje"] != null)
{
    <div class="alert alert-success">@TempData["Mensaje"]</div>
}

<a asp-action="Crear" class="btn btn-primary">Nuevo prospecto</a>

<p class="text-muted mt-3">
    Test prospecto
</p>
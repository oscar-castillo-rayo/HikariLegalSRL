@model HikariLegalSRL.ViewModels.Prospectos.ProspectoEditViewModel

@{
    ViewData["Title"] = "Editar prospecto";
}

@section Styles {
    <link rel="stylesheet" href="~/css/prospectos.css" />
}

<div class="d-flex align-items-center gap-2 mb-4">
    <a asp-action="Index" asp-route-id="@Model.Id" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left"></i>
    </a>
    <h4 class="mb-0 fw-bold prospectos-titulo">Editar prospecto</h4>
</div>

<div class="card shadow-sm border-0 prospectos-form-card">
    <div class="card-body p-3 p-md-4">
        <form asp-action="Editar" method="post"
              data-prospecto-direccion data-pais-base="1"
              data-provincia-actual="@Model.ProvinciaIdActual"
              data-canton-actual="@Model.CantonIdActual"
              data-distrito-actual="@Model.Prospecto.Direccion.DistritoId">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="ProvinciaIdActual" id="provinciaIdActual" />
            <input type="hidden" asp-for="CantonIdActual" id="cantonIdActual" />

            <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small mb-3"></div>

            <div class="row g-3">
                <div class="col-md-6">
                    <label asp-for="Prospecto.NombreEmpresaPersona" class="form-label fw-semibold"></label>
                    <input asp-for="Prospecto.NombreEmpresaPersona" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Prospecto.NombreEmpresaPersona" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Prospecto.NombreContacto" class="form-label fw-semibold"></label>
                    <input asp-for="Prospecto.NombreContacto" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Prospecto.NombreContacto" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Prospecto.CedulaJuridica" class="form-label fw-semibold"></label>
                    <input asp-for="Prospecto.CedulaJuridica" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Prospecto.CedulaJuridica" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Prospecto.Telefono" class="form-label fw-semibold"></label>
                    <input asp-for="Prospecto.Telefono" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Prospecto.Telefono" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Prospecto.Correo" class="form-label fw-semibold"></label>
                    <input asp-for="Prospecto.Correo" type="email" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Prospecto.Correo" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Prospecto.Sector" class="form-label fw-semibold"></label>
                    <input asp-for="Prospecto.Sector" class="form-control" placeholder="Ej: Tecnología, Comercio, Salud..." />
                    <span asp-validation-for="Prospecto.Sector" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Prospecto.Calificacion" class="form-label fw-semibold"></label>
                    <select asp-for="Prospecto.Calificacion" class="form-select">
                        <option value="">-- Sin calificar --</option>
                        @for (byte i = 5; i >= 1; i--)
                        {
                            <option value="@i">@i — @string.Concat(Enumerable.Repeat("⭐", i))</option>
                        }
                    </select>
                    <span asp-validation-for="Prospecto.Calificacion" class="text-danger small"></span>
                </div>
                <div class="col-12">
                    <label asp-for="Prospecto.Observaciones" class="form-label fw-semibold"></label>
                    <textarea asp-for="Prospecto.Observaciones" class="form-control" rows="3"
                              placeholder="Observaciones generales del prospecto..."></textarea>
                    <span asp-validation-for="Prospecto.Observaciones" class="text-danger small"></span>
                </div>

                <div class="col-12">
                    <hr class="my-1" />
                    <small class="text-muted fw-semibold text-uppercase prospectos-seccion">Dirección</small>
                </div>

                <div class="col-12">
                    <div class="form-check form-switch">
                        <input asp-for="EsNacional" class="form-check-input" id="esNacional" />
                        <label class="form-check-label" for="esNacional">Dirección nacional (Costa Rica)</label>
                    </div>
                </div>

                <div class="col-12" id="bloqueNacional">
                    <input type="hidden" asp-for="Prospecto.Direccion.PaisId" id="paisIdOculto" />
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Provincia</label>
                            <select id="selectProvincia" class="form-select">
                                <option value="">-- Seleccione --</option>
                                @foreach (var p in Model.Provincias)
                                {
                                    <option value="@p.Id">@p.Nombre</option>
                                }
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Cantón</label>
                            <select id="selectCanton" class="form-select" disabled>
                                <option value="">-- Seleccione una provincia primero --</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Distrito</label>
                            <select asp-for="Prospecto.Direccion.DistritoId" id="selectDistrito" class="form-select" disabled>
                                <option value="">-- Seleccione un cantón primero --</option>
                            </select>
                            <span asp-validation-for="Prospecto.Direccion.DistritoId" class="text-danger small"></span>
                        </div>
                    </div>
                </div>

                <div class="col-12" id="bloqueExtranjero" style="display:none;">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label fw-semibold">País</label>
                            <select id="selectPaisExtranjero" class="form-select">
                                <option value="">-- Seleccione --</option>
                                @foreach (var pais in Model.Paises)
                                {
                                    <option value="@pais.Id">@pais.Nombre</option>
                                }
                            </select>
                            <span asp-validation-for="Prospecto.Direccion.PaisId" class="text-danger small d-block"></span>
                        </div>
                    </div>
                </div>

                <div class="col-12">
                    <label asp-for="Prospecto.Direccion.SenasExactas" class="form-label fw-semibold"></label>
                    <textarea asp-for="Prospecto.Direccion.SenasExactas" class="form-control" rows="2"
                              placeholder="100m sur del parque central, edificio azul..."></textarea>
                    <span asp-validation-for="Prospecto.Direccion.SenasExactas" class="text-danger small"></span>
                </div>
            </div>

            <div class="d-flex flex-column flex-sm-row justify-content-sm-end gap-2 mt-4">
                <a asp-action="Detalle" asp-route-id="@Model.Id" class="btn btn-outline-danger order-2 order-sm-1">Cancelar</a>
                <button type="submit" class="btn text-white fw-semibold px-4 order-1 order-sm-2 prospectos-btn-guardar">
                    <i class="bi bi-check-lg me-1"></i>Guardar cambios
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/prospectos-direccion.js" asp-append-version="true"></script>
}
